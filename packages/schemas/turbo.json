{
	"$schema": "https://turbo.build/schema.v2.json",
	"extends": ["//"],
	"tasks": {
		"download": {
			"dependsOn": ["^build"],
			"inputs": ["scripts/**"],
			"outputLogs": "new-only",
			"outputs": ["data"]
		},
		"start": {
			"dependsOn": ["^build", "download"]
		},
		"watch": {
			"cache": false,
			"dependsOn": ["^build", "download"],
			"persistent": true
		}
	}
}
